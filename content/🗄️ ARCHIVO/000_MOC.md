# 000_MOC — Guía de Operaciones del Vault + Registro de Archivos

> **Propósito:** Guía completa para gestionar el vault de Obsidian, GitHub, comunicación con Zor, Y registro de últimos archivos creados/modificados.
> **Última actualización:** 20 de mayo de 2026

---

## 📋 ÍNDICE

0. [⚡ Guía de Sincronización](#-guía-de-sincronización) ← LEE ESTO PRIMERO
1. [Registro de Archivos](#registro-de-archivos) ← ACTUALIZADO DIARIAMENTE
2. [Estructura del Vault](#estructura-del-vault)
3. [Plugin de Git en Obsidian](#plugin-de-git-en-obsidian)
4. [Sincronización PC ↔ Móvil ↔ Nube](#sincronización-pc--móvil--nube)
5. [Cómo evitar que Git se corrompa](#cómo-evitar-que-git-se-corrompa)
6. [Comunicación con Zor desde Telegram](#comunicación-con-zor-desde-telegram)
7. [Instrucciones para tareas específicas](#instrucciones-para-tareas-específicas)
8. [Comandos Git de emergencia](#comandos-git-de-emergencia)
9. [Flujo de trabajo recomendado](#flujo-de-trabajo-recomendado)
10. [Solución de problemas comunes](#solución-de-problemas-comunes)
11. [Contacto con Zor](#contacto-con-zor)

---

# ⚡ GUÍA DE SINCRONIZACIÓN

> **Regla de oro:** Siempre haz PULL antes de empezar a editar, y PUSH antes de cerrar.

## Flujo correcto desde Obsidian (PC)

### Al empezar a trabajar:
1. Abrir Obsidian
2. **Ctrl+Shift+P** (Pull — traer cambios de GitHub)
3. Esperar a que sincronice
4. Trabajar en las notas

### Al terminar de trabajar:
1. **Ctrl+Shift+S** (Sincronizar — commit + push)
2. Esperar a que sincronice
3. Cerrar Obsidian

### Al cambiar de dispositivo:
1. En el dispositivo anterior: **Ctrl+Shift+S** (push)
2. En el nuevo dispositivo: **Ctrl+Shift+P** (pull)
3. Trabajar
4. Repetir al cambiar de nuevo

## Flujo correcto desde Telegram (Zor)

Cuando pides algo a Zor por Telegram:
1. Zor trabaja en el VPS
2. Zor hace commit y push a GitHub automáticamente
3. Tú haces **Ctrl+Shift+P** en Obsidian para traer los cambios

**Si Zor te dice que algo se subió a GitHub, siempre haz pull en Obsidian antes de editar ese archivo.**

## Cómo evitar conflictos

1. **SIEMPRE hacer pull antes de empezar a editar**
2. **SIEMPRE hacer push antes de cerrar Obsidian**
3. **NO editar el mismo archivo en dos dispositivos a la vez**
4. **NO forzar el cierre de Obsidian mientras sincroniza**
5. **Si Zor está trabajando, espera a que termine antes de editar**

## Si hay un conflicto (como el de hoy)

Si Obsidian te dice que hay un conflicto:

1. **Cerrar Obsidian completamente**
2. En terminal (en la carpeta del vault):
```bash
git checkout --theirs NOMBRE_DEL_ARCHIVO.md
git add NOMBRE_DEL_ARCHIVO.md
git commit -m "Resolve conflict"
git pull --rebase
git push
```
3. Volver a abrir Obsidian

**¿Qué significa `--theirs`?** Significa "quedarse con la versión de GitHub" (la más reciente). Casi siempre es la correcta.

## Resumen visual

```
OBSIDIAN (PC) ←——pull——→ GITHUB ←——push——→ VPS (ZOR)
     ↑                                              |
     └──────────── Ctrl+Shift+S ────────────────────┘
```

- **Ctrl+Shift+P** = Pull (traer de GitHub a Obsidian)
- **Ctrl+Shift+S** = Push (subir de Obsidian a GitHub)
- **Zor por Telegram** = Trabaja en VPS → push a GitHub → tú haces pull

---

# 🗂️ REGISTRO DE ARCHIVOS

> **Actualización automática** vía Obsidian Bases (plugin nativo, no requiere instalación).

## 🆕 Últimos archivos creados

```base
filters:
  and:
    - 'file.ext == "md"'
formulas:
  created: 'file.ctime.format("YYYY-MM-DD")'
  folder: 'file.folder'
properties:
  file.name:
    displayName: Archivo
  formula.created:
    displayName: Creado
  formula.folder:
    displayName: Carpeta
views:
  - type: table
    name: Últimos creados
    limit: 15
    order:
      - file.name
      - formula.created
      - formula.folder
    sort:
      - property: file.ctime
        direction: DESC
```

## ✏️ Últimos archivos modificados

```base
filters:
  and:
    - 'file.ext == "md"'
formulas:
  modified: 'file.mtime.format("YYYY-MM-DD")'
  folder: 'file.folder'
properties:
  file.name:
    displayName: Archivo
  formula.modified:
    displayName: Modificado
  formula.folder:
    displayName: Carpeta
views:
  - type: table
    name: Últimos modificados
    limit: 15
    order:
      - file.name
      - formula.modified
      - formula.folder
    sort:
      - property: file.mtime
        direction: DESC
```

## 📊 Estadísticas del vault

```base
filters:
  and:
    - 'file.ext == "md"'
formulas:
  folder: 'file.folder'
properties:
  file.name:
    displayName: Archivo
  formula.folder:
    displayName: Carpeta
views:
  - type: table
    name: Archivos por carpeta
    limit: 50
    order:
      - file.name
      - formula.folder
    sort:
      - property: formula.folder
        direction: ASC
```

## 🧹 Pendiente de limpieza

- [ ] Eliminar `000000000000 MOCK.md` (legado, ya fusionado aquí)
- [ ] Eliminar `plugin-test.md.md`
- [ ] Eliminar `prueba-sync.md`

---

# GUÍA DE OPERACIONES DEL VAULT

---

## 1. ESTRUCTURA DEL VAULT

### Ubicación
- **PC de Daniel:** Carpeta del vault (la que sincronizas con GitHub)
- **VPS (Zor):** `/opt/data/workspace/zor-vault/`
- **GitHub:** Repositorio `playerultimate99/obsidian-vault`

### Carpetas principales

```
zor-vault/
├── 000_MOC.md ← ESTE ARCHIVO (guía + registro)
├── Daily/
│   └── 2026/2026-05/
│       ├── 2026-05-19.md
│       └── 2026-05-20.md
├── Transcripciones/
│   ├── 2026-05-18_Jett-Franzen-*.md (6 archivos)
│   ├── 2026-05-19_Daniel-Marzan-Demuestra-Caracter.md
│   ├── 2026-05-19_Experto-Marketing-7-Estrategias.md
│   ├── 2026-05-20_Mentalidad-Levantate-Esfuerzate-DaSilva.md
│   ├── ANALISIS-CRUZADO-Caracter-vs-Esfuerzo.md
│   ├── ANALISIS-SCRIPT-Fracasado-vs-Jett.md
│   ├── NEUROCIENCIA-DEL-FRACASO.md
│   ├── CONEXIONES-GUIONES.md
│   ├── QUOTES-POTENTES.md
│   ├── QUOTES-GENERICAS.md
│   ├── _FUENTES.md
│   └── _REGISTRO.md
├── Research/
│   ├── Negocios/2026-05-20.md
│   ├── Psicologia/2026-05-19.md
│   ├── Marketing/2026-05-18.md
│   └── ...
├── Flashcards/
│   ├── Marketing-2026-05-18.txt
│   ├── Psicologia-2026-05-19.txt
│   └── Negocios-2026-05-20.txt
├── Banco de hooks — Frases rompedoras.md
├── Banco de cierres — Llamadas a la acción.md
├── Banco de Ideas de Guiones.md
├── _ESTRUCTURA-MOTIVERSITY.md
├── _INFORME-MERCADO-MOTIVACION.md
├── _PERFIL-INTERESES.md
└── ...
```

### Convenciones de nombres

| Tipo | Formato | Ejemplo |
|------|---------|---------|
| Transcripción | `YYYY-MM-DD_Autor-Titulo.md` | `2026-05-18_Jett-Franzen-stop-taking-yourself-seriously.md` |
| Guion | `Guion-Titulo.md` | `Guion-Mejor-Escenario-Posible.md` |
| Investigacion | `Tema-Subtema.md` | `Investigacion-Soledad-Moderna.md` |
| Idea | `Idea-Titulo.md` | `Idea-Vivimos-Mejor-Nos-Sentimos-Mas-Infelices.md` |
| Diario | `YYYY-MM-DD.md` | `2026-05-20.md` |
| Audio | `AUDIO - Descripción.md` | `AUDIO - Reflexion sistema cansado.md` |

### Tags principales

| Tag | Uso |
|-----|-----|
| `#guion` | Transcripciones y guiones |
| `#investigacion` | Investigación temática |
| `#idea` | Ideas en bruto o desarrollo |
| `#autor` | Notas sobre autores/filósofos |
| `#cita` | Citas directas de expertos |
| `#formato` | Formato de vídeo |
| `#estrategia` | Estrategia de contenido |
| `#sistema` | Configuración del sistema |
| `#produccion` | Estado de producción |
| `#diario` | Resúmenes diarios |
| `#audio` | Transcripciones de audio |

---

## 2. PLUGIN DE GIT EN OBSIDIAN

### Instalación (PC)

1. Abrir Obsidian → **Configuración** → **Plugins de la comunidad**
2. Buscar **"Obsidian Git"** (por Vinzent03)
3. Instalar y activar
4. Ir a **Configuración del plugin Obsidian Git**

### Configuración del plugin

En **Configuración → Obsidian Git**:

```
✅ Auto backup interval (minutes): 5
✅ Auto pull on startup: Activado
✅ Auto push on activado: Activado
✅ Pull updates on activado: Activado
✅ Commit message: "vault backup: {{date}}"
✅ Subfolder: (vacío, todo el vault)
```

### Conectar con GitHub (primera vez)

1. Crear repositorio en GitHub (privado o público)
2. En terminal (en la carpeta del vault):
```bash
git init
git remote add origin git@github.com:playerultimate99/obsidian-vault.git
git add .
git commit -m "Inicialización del vault"
git push -u origin main
```

3. En Obsidian Git plugin, configurar:
   - **SSH Key:** Generar una clave SSH y añadirla a GitHub
   - **Passphrase:** La que hayas puesto a tu clave SSH

### Uso diario del plugin

- **Ctrl+Shift+S** → Sincronizar (pull + commit + push)
- **Ctrl+Shift+P** → Pull (traer cambios de GitHub)
- **Ctrl+Shift+U** → Push (subir cambios a GitHub)

---

## 3. SINCRONIZACIÓN PC ↔ MÓVIL ↔ NUBE

### Opción A: GitHub (Recomendada)

**PC:**
1. Plugin Obsidian Git configurado (como arriba)
2. Sincronizar antes de cerrar y al abrir

**Móvil (Android):**
1. Instalar **Termux** desde F-Droid
2. En Termux:
```bash
pkg install git
git clone git@github.com:playerultimate99/obsidian-vault.git
```
3. Abrir Obsidian → Crear vault → Seleccionar la carpeta clonada
4. Para sincronizar: abrir Termux y ejecutar:
```bash
cd obsidian-vault
git pull
# (editar notas en Obsidian)
git add .
git commit -m "Update from mobile"
git push
```

**Móvil (iOS):**
1. Usar la app **Working Copy** (cliente Git para iOS)
2. Clonar el repositorio
3. Configurar Working Copy como carpeta de Obsidian
4. Sincronizar desde Working Copy antes y después de editar

### Opción B: Obsidian Sync (de pago)

1. Comprar Obsidian Sync ($4/mes)
2. Activar en Configuración → Sync
3. Crear vault remoto
4. En móvil: Conectar al mismo vault remoto
5. Sincronización automática

### Opción C: Syncthing (gratis, sin nube)

1. Instalar Syncthing en PC y móvil
2. Compartir la carpeta del vault entre dispositivos
3. Sincronización P2P (sin pasar por la nube)
4. **No usar con Git al mismo tiempo** (puede causar conflictos)

---

## 4. CÓMO EVITAR QUE GIT SE CORROMPA

### El problema común

El archivo `.git/index` se corrompe cuando:
- Se cierran dos instancias de Git al mismo tiempo
- Se fuerza el cierre de Obsidian mientras sincroniza
- Hay conflictos de merge no resueltos
- El disco se llena durante una operación Git

### Solución cuando Git se corrompe

**Paso 1: Cerrar Obsidian completamente**

**Paso 2: En terminal (en la carpeta del vault):**
```bash
# Eliminar el índice corrupto
rm -f .git/index

# Reconstruir el índice
git reset

# Verificar que todo está bien
git status
```

**Paso 3: Si hay conflictos de merge:**
```bash
# Ver qué archivos tienen conflictos
git status

# Para cada archivo en conflicto, elegir qué versión mantener:
git checkout --ours archivo.md    # Tu versión local
git checkout --theirs archivo.md  # La versión de GitHub

# O resolver manualmente editando el archivo
# (buscar <<<<<<< HEAD y >>>>>>> para ver los conflictos)

# Después de resolver:
git add .
git commit -m "Resuelto conflicto de merge"
```

**Paso 4: Si nada funciona (nuclear option):**
```bash
# Guardar una copia de seguridad de tus notas
cp -r /ruta/al/vault /ruta/backup-vault

# Eliminar Git y volver a empezar
rm -rf .git
git init
git remote add origin git@github.com:playerultimate99/obsidian-vault.git
git add .
git commit -m "Reinicio del vault"
git push -u origin main --force
```

### Prevención (lo más importante)

1. **SIEMPRE hacer pull antes de empezar a editar**
2. **SIEMPRE hacer push antes de cerrar Obsidian**
3. **NO editar el mismo archivo en dos dispositivos a la vez**
4. **NO forzar el cierre de Obsidian mientras sincroniza**
5. **Si ves un error de Git, NO ignores — resuélvelo inmediatamente**

### Configuración anti-corrupción en Obsidian Git

En la configuración del plugin:
```
✅ Pull updates on startup: SÍ (siempre traer cambios al abrir)
✅ Auto backup interval: 5 minutos
✅ Push on backup: SÍ
✅ Disable push: NO
✅ Show status bar: SÍ (para ver el estado de sincronización)
```

---

## 5. COMUNICACIÓN CON ZOR DESDE TELEGRAM

### Cómo funciona

Zor está conectado a Telegram. Puedes enviarle mensajes desde la app de Telegram y Zor responderá. Zor tiene acceso a:
- El vault de Obsidian (en el VPS)
- Internet (búsqueda, scraping, APIs)
- Herramientas de investigación (Firecrawl, TranscriptAPI, etc.)
- GitHub (para subir/bajar cambios)

### Formato de mensajes para Zor

**Para pedir una transcripción:**
```
Transcribe el video de Jett Franzen: [URL]
```

**Para pedir investigación:**
```
Investiga sobre [tema]. Busca datos, citas de expertos, conexiones con filósofos.
```

**Para pedir citas:**
```
Dame citas potentes sobre [tema] de autores reconocidos.
```

**Para pedir que organice el vault:**
```
Organiza la carpeta [X]. Mueve archivos, crea enlaces, añade tags.
```

**Para pedir que suba a GitHub:**
```
Sincroniza el vault con GitHub.
```

**Para pedir un guion:**
```
Desarrolla una idea para guion sobre [tema]. Usa las transcripciones de Jett y las citas de la biblioteca.
```

### Comandos especiales

| Comando | Acción |
|---------|--------|
| `sync` | Sincroniza el vault con GitHub |
| `status` | Muestra el estado del vault y Git |
| `backup` | Hace un backup completo del vault |
| `search [tema]` | Busca en todo el vault |
| `new [tipo] [título]` | Crea una nueva nota |

---

## 6. INSTRUCCIONES PARA TAREAS ESPECÍFICAS

### Cómo crear una nueva transcripción

1. Daniel envía URL del video de Jett a Zor
2. Zor extrae el transcript con TranscriptAPI
3. Zor crea el archivo en `Transcripciones/` con el formato:
   - Header (título, autor, fecha, URL, vistas)
   - Transcripción literal (inglés)
   - Traducción al español (literal)
   - Conexiones con autores (mínimo 6, enriquecidas)
   - Notas de traducción
4. Zor actualiza `_REGISTRO.md`
5. Zor sincroniza con GitHub

### Cómo crear un nuevo guion

1. Daniel envía la idea (audio o texto) a Zor
2. Zor crea la nota en `Transcripciones/`
3. Zor busca conexiones con transcripciones de Jett
4. Zor busca citas de autores que refuercen los puntos
5. Zor desarrolla la estructura del guion (hook, desarrollo, cierre)
6. Zor sincroniza con GitHub

### Cómo investigar un tema

1. Daniel pide investigación sobre un tema
2. Zor busca en internet (Firecrawl, Exa, etc.)
3. Zor busca conexiones con autores/filósofos
4. Zor crea nota en `Research/`
5. Zor enlaza con guiones y transcripciones relacionadas
6. Zor sincroniza con GitHub

---

## 7. COMANDOS GIT DE EMERGENCIA

### Ver estado del repositorio
```bash
git status
```

### Ver historial de commits
```bash
git log --oneline -20
```

### Deshacer el último commit (sin perder cambios)
```bash
git reset --soft HEAD~1
```

### Deshacer el último commit (perdiendo cambios)
```bash
git reset --hard HEAD~1
```

### Forzar push (SOLO si sabes lo que haces)
```bash
git push --force
```

### For pull (traer cambios remotos)
```bash
git pull --rebase
```

### Ver diferencias entre local y remoto
```bash
git diff origin/main
```

### Crear una rama de emergencia
```bash
git checkout -b emergencia
git add .
git commit -m "Backup de emergencia"
git push -u origin emergencia
```

### Volver a la rama main
```bash
git checkout main
```

---

## 8. FLUJO DE TRABAJO RECOMENDADO

### Al empezar a trabajar (PC o móvil)

1. Abrir Obsidian
2. **Ctrl+Shift+P** (Pull — traer cambios)
3. Esperar a que sincronice
4. Trabajar en las notas

### Al terminar de trabajar

1. **Ctrl+Shift+S** (Sincronizar — commit + push)
2. Esperar a que sincronice
3. Cerrar Obsidian

### Al cambiar de dispositivo

1. En el dispositivo anterior: **Ctrl+Shift+S** (push)
2. En el nuevo dispositivo: **Ctrl+Shift+P** (pull)
3. Trabajar
4. Repetir al cambiar de nuevo

---

## 9. SOLUCIÓN DE PROBLEMAS COMUNES

### "No puedo hacer push"
```bash
# Primero traer cambios
git pull --rebase
# Luego subir
git push
```

### "Hay conflictos de merge"
```bash
# Ver archivos en conflicto
git status
# Resolver manualmente (editar archivos)
# Luego:
git add .
git commit -m "Resuelto conflicto"
git push
```

### "El plugin de Git no funciona"
1. Cerrar Obsidian
2. En terminal:
```bash
cd /ruta/al/vault
rm -f .git/index
git reset
```
3. Abrir Obsidian
4. Intentar sincronizar de nuevo

### "Perdí mis notas"
1. Verificar en GitHub (github.com/playerultimate99/obsidian-vault)
2. Si están en GitHub:
```bash
git pull --force
```
3. Si no están en GitHub, verificar la papelera de reciclaje del PC
4. Si no están en ningún lado, contactar a Zor (puede tener una copia en el VPS)

---

## 10. CONTACTO CON ZOR

### Desde Telegram

Simplemente escribe tu petición. Zor está disponible 24/7.

**Ejemplos de peticiones:**
- "Transcribe el último video de Jett"
- "Investiga sobre la soledad moderna"
- "Dame citas sobre la ambición"
- "Sincroniza el vault"
- "Crea una nota sobre [tema]"

### Tiempos de respuesta

| Tarea | Tiempo estimado |
|-------|-----------------|
| Transcripción de video | 2-5 minutos |
| Investigación simple | 5-10 minutos |
| Investigación profunda | 15-30 minutos |
| Organización del vault | 10-20 minutos |
| Sincronización GitHub | 1-2 minutos |

---

*Documento mantenido por Zor — Actualizar cuando haya cambios en el sistema*
*Última actualización del registro: 2026-05-20*
